#!/bin/bash

terminus_loop () {
  # 0 is always dev, on dev never attempt to deploy.
  if (( $1 == 0 )); then
    $terminus -y drush "$site.$2" -- updb -y && $terminus drush "$site.$2" cr && $terminus drush "$site.$2" -- cim -y
  else
    $terminus -y env:deploy "$site.$2" && $terminus drush "$site.$2" -- updb -y && $terminus drush "$site.$2" cr && $terminus drush "$site.$2" -- cim -y
  fi
}

# Check that terminus command exists.
if type $terminus >/dev/null 2>&1; then
  if [[ $terminusValue ]]; then
    declare steps=('dev' 'test' 'live')
    site=${terminusValue%.*}
    env=${terminusValue#*.}

    if [[ $site && $env ]]; then
      case $env in
        'dev')
          count=0
          ;;
        'test')
          count=1
          ;;
        'live')
          count=2
          ;;
        *)
          echo
          echo 'Atempting to run against multidev environment.'
          echo
          echo 'If this was not intended, make sure to specify dev, test or live.'
          echo
          multidev=$env
      esac

      i=0
      if [[ $count ]]; then
        while [[ $i -le $count ]]
        do
          terminus_loop "$i" "${steps[$i]}"
          ((i++))
        done
      else
        terminus_loop "$i" "$multidev"
      fi
    fi
  fi
fi